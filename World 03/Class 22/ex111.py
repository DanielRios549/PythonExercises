'''
    Create a package called coin that have the modules inside it.
    Move all functions created from exercise 107 to 109 to these modules without breaking.
'''
import coin

number = float(input('Choose a price: R$ '))
coin.summary(number, 10, 35)
